# Archived control variants

`robot_variants/` contains the earlier robot-folder navigation and lane/platform implementations. `manuscript/` preserves the distinct manuscript copies. They are retained for provenance, not presented as the primary entry points.

Use the five applications described in the [main program guide](../docs/programs.md). Do not transfer calibration constants between versions without reviewing their behavior.
